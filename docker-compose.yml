version: "3.9"

services:
  backend:
    build: ./backend
    container_name: flask-backend
    ports:
      - "5000:5000"

  frontend:
    build: ./frontend
    container_name: express-frontend
    ports:
      - "3001:3001"
    depends_on:
      - backend
